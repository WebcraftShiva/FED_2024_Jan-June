let num = [1,2,3,4]

num.shift();             // Removing value from the array "num" from first. Passing Argument is not required.
console.log(num);

num.unshift("9")            // Adds element at the en of the array. Passing argument is necessary here
console.log(num);